<%= form_with(model: book_type) do |form| %>
  <% if book_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_type.errors.count, "error") %> prohibited this book_type from being saved:</h2>

      <ul>
        <% book_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :typeName, style: "display: block" %>
    <%= form.text_area :typeName %>
  </div>

  <div class="pt-3">
    <%= form.submit %>
  </div>
<% end %>
